# 教材征订系统 · Web 端 PRD

## 概念版 PRD（≤ 200 字）

核心用户：高校教材室管理员、学院秘书、任课教师、学生与教材供货商（5 角色）。
要解决的一件事：每学期教材征订靠纸质表格与群接龙，填报分散、汇总困难、通知确认无法追踪。
产品形态：Web 端（5 角色全支持）+ 微信小程序（教师/学生，另见小程序 PRD）。
最小可用版功能（≤ 3 条）：
  1. 征订窗口期内教师填报教材需求、学生选购教材（系统字段审查+超管内容复核，关窗锁定）
  2. 按角色数据范围查看与导出（RBAC + 学院隔离，供货商只读且屏蔽学生数据）
  3. 通知确认闭环（打开即阻塞弹窗，点「收到」放行；未确认走订阅消息每日重发，最多 5 轮）
本版本不做：支付环节、教学班（重修/自修）、教材库存管理、AI 功能。
商业模式：校内免费。
技术前提：账号由超管分发（不可自主注册）、学期数据 Excel 全量导入、双缓冲学期切换；后端 Spring Boot 3 + MySQL 8（详见 textbook-order-server 仓库）。
> 各功能的异常分支、状态机、字段校验规则与文案规范详见落地版。

---

## 落地版 PRD

以下按概述 / 功能列表 / 页面结构 / 功能说明组织；每个功能均定义异常分支、状态机、字段校验规则与文案规范，全局令牌见功能 7。

### 一、概述

**文档信息**

| 版本号 | 日期 | 作者 |
|--------|------|------|
| V 1.0.0 | 2026-09-17 | ZhangJing-gugugaga |

#### 1. 需求背景

- **目标用户现状**：高校教材征订由教材室组织，依赖班级纸质表格、群接龙与人工 Excel 汇总；任课教师报教材需求靠邮件/微信群，学院秘书汇总后线下签字。
- **核心痛点**：填报分散易错、汇总滞后、通知触达与确认无记录、多角色数据权限靠人工把握易泄露。
- **当前问题汇总**：

| 问题点 | 问题描述 | 当前影响 |
|--------|----------|----------|
| 填报分散 | 教师需求、学生选购走线下多渠道 | 数据口径不一，返工核对 |
| 汇总困难 | 人工合并 Excel，按学院/课程多维度统计 | 统计滞后，易错漏 |
| 通知无闭环 | 通知发了不知道谁没收到 | 漏订、催办靠群刷屏 |
| 数据权限 | 学生数据可能被无关方接触 | 隐私风险 |

> 上述痛点决定了各功能必须内建异常分支与数据校验；状态机与文案规范统一在「四、功能说明」与功能 7 汇总。

#### 2. 需求目标

- **业务目标**：
  1. 教师填报与学生选购 100% 线上完成，征订数据一次汇总；
  2. 5 角色数据隔离零越权（供货商物理屏蔽学生数据）；
  3. 通知确认率可追踪、可导出（未确认者自动重发至多 5 轮）。
- **成功标准**：

| 指标 | 当前状态 | 目标状态 |
|------|----------|----------|
| 线上填报率 | 0（纸质） | ≥ 95% |
| 征订汇总耗时 | 数天人工 | 实时可查，导出 ≤ 1 分钟 |
| 通知确认可追踪 | 无 | 每生每轮发送/确认时间全落库 |
| 越权事件 | 无法审计 | 全量审计日志 + 越权测试用例全过 |

> 为达成目标，各功能的异常分支处理、字段校验规则与用户文案规范在第四章逐项定义。

#### 3. 需求范围

- **包含**：5 角色 Web 端全部功能（见功能列表）；RBAC 动态路由与按钮级权限；征订窗口引擎三态展示；Excel 异步导入交互；各角色导出；阻塞弹窗通知确认。
- **不包含**：支付；教学班（重修/自修）场景；教材库存/出入库；AI 功能；小程序端（独立 PRD）；后端接口实现（见 textbook-order-server）。
- 范围内各功能的异常分支、状态机、字段校验规则与提示文案，在「四、功能说明」逐项定义；各页面通用的异常处理与文案令牌见功能 7。

### 二、功能列表

| 模块 | 功能名称 | 子任务 | 功能说明 | 优先级 |
|------|----------|--------|----------|--------|
| 公共 | 登录 | 账号密码登录 | 用户可凭学号/工号+密码登录，按角色进入对应工作台 | P0 |
| 公共 | 强制改密 | 首登改密 | 初始密码（学号/工号后 6 位）用户登录后必须先改密才能使用任何功能 | P0 |
| 公共 | 个人中心 | 改密码/切换身份 | 用户可自助改密码；多角色用户可切换身份重载菜单 | P0 |
| 教材室 | 账号管理 | 建号/停用/重置 | 超管可手动创建账号（含供货商）、停用启用、重置密码 | P0 |
| 教材室 | 组织管理 | 学院/专业/班级 | 三级组织树维护 | P0 |
| 教材室 | 征订窗口管理 | 设置/自动开关/延长/提前截止 | 超管设置学期与窗口起止（天数任意）；到点自动开/关；可无限次延长或提前截止，提前截止后可再延长 | P0 |
| 教材室 | 教材库维护 | CRUD/导入/停用 | 教材按 ISBN 唯一，跨学期共用，可停用旧书 | P0 |
| 教材室 | 课程与任课管理 | 课程维护/关联导入 | 课程与教师-课程-班级任课关系维护与 Excel 导入 | P0 |
| 教材室 | 学生/教师管理 | 全量导入/检索/审批 | 全量 Excel 异步导入（批次进度+错误行下载）；学生/教师异动两级审批（系统字段审查结果展示→内容审核通过/驳回，理由必填） | P0 |
| 教材室 | 复核工作台 | 两级审查视图 | 教师表单先看系统字段审查结果（只读），再做内容审核通过/驳回（理由必填）；被驳回表单解锁补正状态可见 | P0 |
| 教材室 | 征订数据 | 教师明细/学生选购 | 全院征订明细多维度筛选查看 | P0 |
| 教材室 | 导出中心 | 多维度导出 | 全院征订明细、通知汇总等导出（后端生成文件流） | P0 |
| 教材室 | 通知管理 | 任务配置/进度 | 手动建通知任务；系统自动任务（窗口变更）只读展示；发送/确认进度与失败名单 | P1 |
| 学院秘书 | 本院征订记录 | 只读查看 | 仅本院教师提交的征订单及明细，只读 | P0 |
| 学院秘书 | 本院导出 | 签字版导出 | 导出本院征订表格（含签字栏版式）用于线下签字 | P0 |
| 学院秘书 | 异动申请 | 提交/进度 | 逐条或 Excel 提交学生异动，查看两级审批进度 | P0 |
| 任课老师 | 我的课程 | 课程带出 | 按导入的任课关系带出本人课程，按班级分组 | P0 |
| 任课老师 | 填报教材 | 选教材/填数量/重提 | 窗口内逐课程从教材库选教材填数量；提交先过系统字段审查（错误逐字段提示可修复重提）再转超管内容审核；被驳回状态醒目+补正入口；关窗锁定 | P0 |
| 任课老师 | 我的提交记录 | 历史/审查轨迹 | 历史学期列表+明细+审查状态轨迹；无导出 | P0 |
| 任课老师 | 异动申请 | 提交学生异动 | 教师可提交学生异动，进入两级审批 | P1 |
| 学生 | 选书 | 勾选/数量/提交 | 窗口内按班级带出教材清单，勾选/取消、数量步进，提交=覆盖更新；一人一本提醒沿用 MVP；提交确认弹窗底部固定小字「价格和版本以最终出版单位供应为准」 | P0 |
| 学生 | 我的选购记录 | 历史查看 | 历史学期选购记录查看，无导出 | P0 |
| 供货商 | 订购清单 | 只读查看 | 按学院分组查看，仅 书名/ISBN/教师姓名/学院 四类字段 | P0 |
| 供货商 | 清单导出 | 分学院 sheet | 一次导出、一个学院一个 sheet；每次导出后端审计留痕 | P0 |
| 全员 | 阻塞弹窗通知 | 确认放行 | 打开 Web 拉取未确认通知，自绘居中 Modal 逐条弹出，点「收到」确认后放行进入；无未确认通知直接进入 | P1 |

> 各功能的提示文案统一走全局文案令牌（见功能 7 文案规范表）；每个表单的字段校验规则在「四、功能说明」对应功能中给出。

### 三、页面结构与导航设计

本章各页面的异常分支、状态机、字段校验与提示文案以功能 7 的全局规范为基线，6 个主要页面单独定义。

#### 整体导航结构

```
[类型：登录页 + 登录后侧边栏布局（按角色动态生成）]
├─ 登录页 → 账号密码登录/强制改密引导
├─ 个人中心 → 改密码、切换身份（多角色）
├─ 数据看板（超管） → 全局进度与窗口状态
├─ 账号管理（超管） → 账号全生命周期
├─ 组织管理（超管） → 学院/专业/班级树
├─ 征订窗口管理（超管） → 学期与窗口引擎
├─ 教材库（超管） → 教材 CRUD 与导入
├─ 课程与任课管理（超管） → 课程与任课关系
├─ 学生/教师管理（超管） → 导入/检索/异动审批
├─ 复核工作台（超管） → 教师表单两级审查
├─ 征订数据（超管） → 全院明细
├─ 导出中心（超管） → 全院导出
├─ 通知管理（超管） → 通知任务与进度
├─ 本院征订记录（秘书） → 只读+导出
├─ 异动申请（秘书/教师） → 提交与进度
├─ 我的课程/填报教材/我的提交记录（教师）
├─ 选书/我的选购记录（学生）
└─ 订购清单（供货商） → 只读+导出
```

菜单可见性由权限码动态生成；无权限路由访问被守卫重定向 403 页。各页面的异常反馈、字段校验与提示文案统一遵循功能 7 的全局规范；以下 6 个主要页面给出专属设计。

#### 选书页（学生）

**页面职责：** 窗口内按班级完成教材勾选与提交。

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 窗口横幅 | 窗口三态（未开始/进行中含倒计时/已截止） | 始终 |
| 教材清单 | 本班教材卡片：书名/ISBN/版次/作者/出版社/单价/必修标识 | 有任务数据 |
| 底部结算栏 | 已选品种/数量/合计金额 + 提交按钮 | 有勾选时 |
| 提交确认弹窗 | 明细核对 + 底部小字「价格和版本以最终出版单位供应为准」 | 点提交时 |
| 截止遮罩 | 「本期征订已截止，可查看历史记录」 | 窗口关闭 |

**异常分支：** 提交时若窗口刚好关闭（后端 409），提示「本期征订已截止，提交未生效」；提交失败保留本地勾选不丢失。
**状态机：** 未提交 → 已提交（覆盖更新语义，重提=整单替换）→ 已截止（锁定）。
**字段规范：** 数量步进范围校验为 1-9，减到 0 自动取消勾选；必修教材默认勾选 1 本。
**文案规范：** 空态「本班暂无征订书目」；一人一本提醒「你勾选了多种教材，请确认是否需要多本」；成功「提交成功，可在我的选购记录查看」。
**本页面不展示：** 其他学生记录、导出按钮、修改他人数据的任何入口。

#### 填报教材页（教师）

**页面职责：** 窗口内按课程完成教材需求填报与补正。

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 课程分组 | 本人课程×班级分组列表 | 任课关系存在 |
| 教材选择器 | 教材库搜索（书名/ISBN）选中后入明细 | 窗口开启 |
| 明细编辑 | 数量步进、删除行 | 窗口开启 |
| 审查状态条 | 系统字段审查结果（逐字段错误提示）/ 内容审核状态 | 已提交 |
| 驳回补正横幅 | 「表单被驳回，理由：…，已解锁可补正重提」 | 状态=rejected |
| 截止遮罩 | 同选书页 | 窗口关闭 |

**异常分支：** 字段审查失败逐字段标红并给出修复提示；课程无任课关系时显示空态引导联系教材室。
**状态机：** draft → field_check → pending_review → reviewed / rejected（rejected 解锁仅该表单补正重提）。
**字段规范：** 每行明细=课程×班级×教材×数量；数量必填 >0 且 ≤ 班级人数上限；教材必须为在库状态。
**文案规范：** 提交成功「已提交，等待复核」；审查失败「存在 N 项问题，请按提示修复后重新提交」。
**本页面不展示：** 他人表单、导出按钮、绕过两级审查的提交路径。

#### 复核工作台（超管）

**页面职责：** 教师表单两级审查的第二级人工审核。

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 待审列表 | 表单摘要+提交时间 | 有 pending_review 表单 |
| 系统字段审查结果 | 自动审查结论（只读展示，逐项通过/失败） | 选中表单 |
| 明细预览 | 课程×班级×教材×数量 | 选中表单 |
| 审核操作 | 通过 / 驳回（理由必填输入框） | 选中表单 |

**异常分支：** 驳回理由为空时禁止提交驳回；复核与教师补正并发时以后端状态机裁决，冲突提示刷新。
**状态机：** pending_review → reviewed（通过）/ rejected（驳回，表单解锁补正）。
**字段规范：** 驳回理由 1-200 字必填。
**文案规范：** 通过「已复核通过」；驳回「已驳回，教师可补正后重新提交」。
**本页面不展示：** 修改表单内容的编辑能力（复核不改数据，驳回由教师补正）。

#### 征订窗口管理页（超管）

**页面职责：** 学期与窗口引擎唯一控制入口。

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 学期列表 | 学期名/起止日期/状态（draft/active/archived） | 始终 |
| 窗口设置 | 窗口起止时间（天数任意）、自动开启/自动截止开关 | 选中学期 |
| 操作区 | 立即开启 / 提前截止 / 延长（修改截止时间） | active 学期 |
| 二次确认弹窗 | 说明操作影响 + 「将自动通知全员」提示 | 每次操作 |
| 变更记录 | 本学期窗口变更历史（谁/何时/原值→新值） | 始终 |

**异常分支：** 延长时间早于当前时间被校验拦截；切换 draft→active 失败时回退显示并提示重试。
**状态机：** 学期 draft → active → archived；窗口 not_open → open → closed →（延长）open。
**字段规范：** 窗口开始 < 结束；天数任意（20 天仅为本次建议值）；同一时刻仅一个 active 学期。
**文案规范：** 操作成功「已生效，通知将自动发送给全员」；二次确认「确认提前截止？截止后填报与选购将锁定」。
**本页面不展示：** 手动修改他人审批中的数据；多学期同时激活。

#### 订购清单页（供货商）

**页面职责：** 只读查看与导出分学院订购清单。

| 区域 | 内容 | 显示条件 |
|------|------|----------|
| 学院筛选 | 学院切换（仅切换视角，无编辑） | 始终 |
| 清单表格 | 书名 / ISBN / 教师姓名 / 所属学院 | 始终 |
| 导出按钮 | 一次导出、按学院分 sheet | 始终 |

**异常分支：** 导出失败提示重试；无数据学院导出空 sheet 并提示。
**状态机：** 无业务状态（纯只读视图），仅有加载中/加载失败/空态三态。
**字段规范：** 列固定四项，不可增删；筛选仅学院维度。
**文案规范：** 空态「该学院暂无征订数据」；导出成功「已下载，导出行为已记录」。
**本页面不展示：** 学生任何字段（姓名/学号/班级/选购记录）、任何新增/修改/删除入口、教材库与账号管理入口。

**其余页面**（账号管理、组织管理、教材库、课程与任课、学生/教师管理、征订数据、导出中心、通知管理、本院记录、异动申请、我的课程、提交记录、选购记录、个人中心）：职责见功能列表，结构均为「筛选区+表格+分页+操作列」标准列表页，异常反馈/字段校验/提示文案统一遵循功能 7 全局规范，各自「不展示」内容按角色权限矩阵（详见《02-前端开发计划与决策》§2/§6）由权限码与后端数据范围共同保证。

#### 功能 7：全局异常与文案令牌

**异常情况处理（全局）**

| 异常场景 | 触发条件 | 处理方式 | 用户反馈 |
|----------|----------|----------|----------|
| 网络断开 | 请求失败 | 请求层统一拦截 | 「网络异常，请稍后重试」 |
| 会话过期 | 401 | 跳登录页 | 「登录已过期，请重新登录」 |
| 无权限访问 | 403 | 路由守卫重定向 | 403 页 |
| 关窗瞬间提交 | 409 | 拦截提交 | 「本期征订已截止，提交未生效」 |
| 数据为空 | 无内容 | 空态插图 | 各页空状态文案 |
| 服务错误 | 5xx | 错误页+重试 | 「服务开小差了，请重试」 |

**字段规范：** 本功能无业务输入字段；各接口的字段校验规则随各功能定义，必填与格式错误的反馈统一遵循上表文案。

**文案规范（全局六类）**

| 场景 | 文案内容 |
|------|----------|
| 空状态 | 暂无数据 |
| 加载中 | 加载中… |
| 操作成功 | 操作成功 |
| 操作失败 | 操作失败，请重试 |
| 权限不足 | 无权访问该页面 |
| 网络异常 | 网络异常，请稍后重试 |

### 四、功能说明

以下每个功能均包含功能描述、用户流程、异常分支、状态机、字段校验规范与文案规范；全局异常处理与文案令牌见功能 7。

#### 功能 1：登录与强制改密

**功能描述**：用户在登录页凭学号/工号+密码登录；初始密码用户首次登录后强制改密。

**用户流程**
```
[登录页]
  ↓ 输入账号+密码
  ├─ 成功 → must_change_password=true?
  │        ├─ 是 → 强制改密弹窗（不可跳过）→ 改密成功 → 进入工作台
  │        └─ 否 → 进入工作台
  └─ 失败 → 红色错误提示（账号或密码不正确）
```

**异常分支说明**：
- 连续失败 5 次：账号锁定 15 分钟，提示「账号已锁定，请稍后再试」；
- 停用账号：提示「账号已停用，请联系教材室」；
- 会话过期：全局 401 拦截跳登录（见功能 7）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 正常 | 登录成功 | 退出/过期/停用 | 正常使用 |
| 待改密 | 初始密码首登 | 改密成功 | 强制改密弹窗，不可跳过 |
| 锁定 | 连续失败 5 次 | 15 分钟后自动解锁 | 登录被拒 |
| 停用 | 超管停用/学期导入比对停用 | 超管启用 | 登录被拒 |

**字段规范**

| 字段名 | 类型 | 必填 | 长度限制 | 校验规则 | 错误提示 |
|--------|------|------|----------|----------|----------|
| 账号 | 文本 | 是 | 32 | 学号/工号格式 | 请输入正确的账号 |
| 密码 | 密码 | 是 | 6-64 | 非空 | 请输入密码 |
| 新密码 | 密码 | 是 | 8-64 | ≥8 位含字母和数字 | 新密码需 8 位以上且含字母和数字 |
| 确认新密码 | 密码 | 是 | 8-64 | 与新密码一致 | 两次输入不一致 |

**文案规范**

| 场景 | 文案内容 |
|------|----------|
| 加载中 | 登录中… |
| 操作成功 | 登录成功 / 密码修改成功 |
| 操作失败 | 账号或密码不正确 |
| 权限不足 | 无权访问该页面 |
| 网络异常 | 网络异常，请稍后重试 |

#### 功能 2：时间窗口三态（全局）

**功能描述**：所有填报/选购入口受窗口引擎三态控制；查看与导出不受窗口限制。

**异常分支：** 状态轮询失败时按上次已知状态渲染并静默重试；关窗瞬间提交由后端 409 兜底（前端不信任本地时钟做最终校验）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| not_open | 当前时间 < window_start 或通道未开启 | 到点自动开/超管手动开 | 显示开始时间倒计时，填报/选购入口置灰 |
| open | 通道开启且 start ≤ now ≤ end | 到点自动截止/超管提前截止 | 入口正常，显示截止倒计时 |
| closed | now > window_end 或超管提前截止 | 超管延长（新截止时间） | 入口锁定+「本期征订已截止，可查看历史记录」横幅；查看/导出不受影响 |

**字段规范：** 倒计时基于服务端返回的 window_end 与服务端时间差计算，避免客户端改表绕过。

**文案规范：** not_open「距离开始还有 X 天 X 时 X 分」；open「本期征订将于 X 截止」；closed「本期征订已截止，可查看历史记录」。

窗口延长/提前截止后，全局横幅更新并自动弹出系统通知（见功能 4）。

#### 功能 3：教师填报（含两级审查）

**功能描述**：教师在窗口内完成教材需求填报；提交后先过系统字段审查（自动），再转超管内容审核（人工），任一环节不通过即驳回。

**用户流程**
```
[我的课程] 选课程
  ↓
[填报] 选教材→填数量→提交
  ↓
系统字段审查（自动：必填/数量>0/教材在库/课程归属/关联存在/ISBN 格式）
  ├─ 失败 → 逐字段错误提示 → 修复后可再次提交（窗口内不限次）
  └─ 通过 → 转超管内容审核（pending_review）
              ├─ 通过 → reviewed，计入汇总
              └─ 驳回（理由必填）→ 表单解锁，仅该表单可补正重提 → 再次提交再审
```

**异常分支说明**：提交时窗口关闭 → 409 拦截；字段审查规则为白名单客观项（教师可控可修复），保证不存在"无论如何提交都被驳回"的死局；超管驳回必填理由。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| draft | 创建/修改中 | 提交 | 可编辑 |
| field_check | 提交 | 审查完成 | 提交中… |
| rejected_auto | 字段审查失败 | 修复重提 | 逐字段错误提示 |
| pending_review | 字段审查通过 | 超管审结 | 已提交，待复核 |
| reviewed | 超管通过 | — | 已复核 |
| rejected | 超管驳回 | 教师补正重提 | 驳回理由+补正入口 |

**字段规范**

| 字段名 | 类型 | 必填 | 限制 | 校验规则 | 错误提示 |
|--------|------|------|------|----------|----------|
| 教材 | 搜索选择 | 是 | 每课程行 | 必须为在库教材（ISBN 匹配） | 请从教材库选择 |
| 数量 | 数字 | 是 | 1-班级人数上限 | 整数 >0 | 数量需大于 0 |
| 补正说明 | 文本 | 否 | 200 字 | 重提时可附说明 | — |

**文案规范：** 成功「已提交，等待复核」；审查失败「存在 N 项问题，请按提示修复后重新提交」；驳回「表单被驳回：{理由}，已解锁可补正重提」。

#### 功能 4：阻塞弹窗通知确认

**功能描述**：打开 Web 拉取未确认通知，居中阻塞 Modal 逐条弹出，点「收到」确认后放行进入。

**用户流程**
```
[打开 Web / 切换身份]
  ↓ GET /notice/unconfirmed
  ├─ 无 → 直接进入
  └─ 有 → 自绘居中 Modal 逐条弹出（非浏览器原生弹窗）
           ↓ 点「收到」
           POST confirm → 全部确认完毕 → 放行进入
           └─ 确认接口失败 → 弹窗保留可重试，不放行
```

**异常分支说明**：确认接口失败时弹窗保留并可重试；通知列表接口失败按"暂无通知"降级但下次进入重查。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 未读 | 通知任务创建/窗口变更自动创建 | 点「收到」 | 打开即弹出，阻塞进入 |
| 已确认 | 点「收到」（confirmed_at 落库，首次生效） | — | 不再弹出；后端停止对该用户重发 |

**字段规范：** 确认请求无输入字段；服务端校验任务归属与幂等（重复确认仅首次生效）。

**文案规范：** 弹窗标题「新通知」；按钮「收到」；确认成功后自动关闭弹窗队列下一条。

#### 功能 5：Excel 异步导入（学生/教师/教材库/课程任课/异动）

**功能描述**：上传 .xlsx 后异步解析，进度可查，错误行可下载。

**用户流程**
```
[上传 .xlsx] → 返回批次号 → 进度条轮询（progress_pct）
  ├─ 完成（含部分错误）→ 结果摘要 + 错误行表格回显 + 错误明细下载
  └─ 失败 → 错误提示 + 重新上传
```

**异常分支说明**：文件超限/类型错误在前置拦截；解析中断批次标记 failed 可重传；同一文件重复上传按幂等 upsert（user_no/ISBN 存在则更新）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 用户可见表现 |
|----------|----------|----------|--------------|
| 解析中 | 上传成功 | 解析完成/失败 | 进度条 |
| 已完成（含错误） | 解析完成且 error>0 | — | 摘要+错误行下载 |
| 全部成功 | 解析完成且 error=0 | — | 成功摘要 |
| 失败 | 解析异常 | 重新上传 | 失败原因 |

**字段规范（上传控件）**

| 字段名 | 类型 | 必填 | 限制 | 校验规则 | 错误提示 |
|--------|------|------|------|----------|----------|
| 文件 | 文件 | 是 | .xlsx，≤10MB | 服务端逐行严格校验（不自动创建学院/班级） | 仅支持 .xlsx 文件 |

**文案规范：** 进行中「正在解析，已完成 X%」；完成「导入完成：成功 X 行，失败 Y 行」；失败「解析失败：{原因}」。

#### 功能 6：供货商只读与导出

**功能描述**：供货商角色全部页面无写操作；清单仅四类字段；导出按学院分 sheet。

**异常分支说明**：越权请求学生字段在后端被物理隔离（接口层不返回），前端页亦无任何学生字段渲染路径。

**状态机：** 无业务状态（纯只读），仅有加载中/失败/空态。

**字段规范**

| 字段名 | 类型 | 必填 | 限制 | 校验规则 | 错误提示 |
|--------|------|------|------|----------|----------|
| 学院筛选 | 下拉 | 否 | 全部/单个学院 | 后端数据范围过滤 | — |
| 导出 | 按钮 | — | 一次导出 | 每次导出后端写审计日志（谁/何时/范围） | 导出失败请重试 |

**文案规范：** 空态「该学院暂无征订数据」；导出成功「已下载，导出行为已记录」。

---

## 五、六大盲区自检清单

- [x] **页面结构**：导航树 + 6 个主要页面职责/元素/不展示明确，其余标准列表页职责在功能列表中明确。
- [x] **异常交互**：功能 1/3/5/6 均含异常路径；功能 7 全局异常表覆盖 401/403/409/断网/空态/5xx。
- [x] **状态机**：窗口三态、教师表单六态、通知确认、导入批次、登录账号状态全部列出。
- [x] **字段规范**：登录/改密/填报/上传/导出字段齐备；校验规则逐字段定义，业务表单字段由后端契约冻结（M1）后回填本表。
- [x] **文案规范**：功能 7 定义全局六类文案令牌，各功能专属文案随功能定义。
- [x] **流程图**：功能 1/3/4/5 流程含成功/失败分支与异常出口。

## 六、安全设计自检清单

**账户安全**
- [x] **人机验证**：校内系统，登录以「失败 5 次锁 15 分钟」+ 服务端限频替代验证码（V1）；暴露公网后出现撞库再引入图形验证码。
- [x] **频率限制**：登录接口 IP+账号双维度限频；导出接口限频。
- [x] **全局熔断**：后端网关层限流（见 textbook-order-server PRD）。

**内容安全**
- [x] **UGC 审核**：本系统用户不产生公开 UGC（填报/选购数据仅授权角色可见）；教师表单内容有超管内容审核环节，先审后汇总。

**文件安全**
- [x] **文件类型限制**：导入仅 .xlsx；魔数校验在服务端。
- [x] **文件大小限制**：单文件 ≤10MB（上万行场景，超出分批）。
- [x] **存储方案**：上传文件落服务端临时目录，导入完成后清理。
- [x] **防盗链**：导出文件流带一次性授权 token，不暴露静态路径。

**AI 安全**
- [x] 本系统无 AI 功能，不适用。

**传输安全**
- [x] **HTTPS**：全站强制（moonzj.com 已备案，Nginx 443）。
- [x] **敏感数据**：密码 BCrypt 存储、HTTPS 传输；名单不含身份证字段（G1 定稿）。
- [x] **CORS**：Web 同域反代 `/api`，无跨域面。

**基础设施安全**
- [x] **环境变量**：DB/AppSecret/JWT 密钥全部环境变量注入。
- [x] **依赖审计**：Maven 依赖定期漏洞扫描。
- [x] **日志脱敏**：日志不含密码/token；导出行为仅记操作者与范围。

> 以上各项的服务端校验规则与异常处理在 textbook-order-server PRD 落地；提示文案统一走功能 7 全局文案令牌；账号锁定与停用状态机见功能 1。

自检通过："PRD 落地版已完成，六大盲区 + 安全设计 + AI 增强 8 大区块均已覆盖。"

---

## 七、问题校验（AI 增强）

- [x] **目标用户是否太泛**：已收敛为校内 5 类明确角色，账号超管分发制，天然可触达。
- [x] **MVP 是否过大**：P0 聚焦「填报-审查-选购-导出」主线；通知弹窗/异动审批 P1；openid 免密等移入 V1.1 停车场。
- [x] **需求是否单一**：本版本只解决「征订流程线上化+数据隔离+通知确认」一件事。
- [x] **验证可行性**：技术前提已冻结（Spring Boot 3 + MySQL 8 + Vue 3；域名已备案；账号分发制；Excel 导入模板 5 张）。

## 八、输入设计（AI 增强 · 复用于文件输入）

| 输入项 | 来源 | 类型 | 是否必填 | 约束/校验规则 | 示例 |
|--------|------|------|----------|---------------|------|
| Excel 文件 | 用户上传 | 文件 | 是 | .xlsx ≤10MB；行级严格校验；幂等 upsert（user_no/ISBN） | 学生全量表.xlsx |
| 窗口时间 | 超管输入 | 日期时间 | 是 | start < end；同刻仅一个 active 学期 | 2026-09-20 08:00 ~ 2026-10-10 18:00 |
| 审核驳回理由 | 超管输入 | 文本 | 驳回时必填 | 1-200 字 | 「数量与班级人数不符，请核对」 |

- [x] 输入缺失时给出兜底提示（文件空/字段空均逐项提示）。
- [x] 输入异常（空/超长/格式错）处理路径已定义（逐行错误收集+可下载明细）。

## 九~十二、AI 输出设计 / AI Workflow / AI 职责拆解 / Badcase 分析（AI 相关区块）

**本系统无 AI 功能，AI 输出设计、AI Workflow、AI 职责拆解不适用。** 系统内所有"自动判断"（字段审查、自动开关窗、重发轮次）均为确定性规则代码，规则白名单化并文档化（见 textbook-order-server PRD §两级审查），不存在模型幻觉风险面。

**Badcase 分析（按模板要求 ≥8 个，采用系统边界/异常场景，非 AI 场景）：**

| 场景 | 可能原因 | 风险等级 | 应对策略 | 是否需要人审 |
|------|----------|----------|----------|--------------|
| [场景1-关窗瞬间提交] | 前后端时钟差/请求在途 | 高 | 服务端 409 裁决，前端友好提示 | 否 |
| [场景2-重复提交覆盖] | 用户双击/网络重试 | 中 | 提交幂等，重提=整单覆盖（idempotency） | 否 |
| [场景3-越权访问别院数据] | 改 ID 直接请求 | 高 | 后端数据隔离切面+IDOR 用例回归 | 否（测试必测） |
| [场景4-供货商请求学生字段] | 接口探测 | 高 | /supplier-api 物理隔离，无学生字段返回路径 | 否（测试必测） |
| [场景5-万行导入中途失败] | 内存/超时/格式异常 | 中 | 批次状态机 failed 可重传；错误行收集不中断 | 是（错误行人工核对） |
| [场景6-窗口延长时在线用户] | 状态轮询滞后 | 低 | 全局横幅+系统通知双通道同步 | 否 |
| [场景7-驳回后始终不补正] | 教师失联/弃单 | 中 | 导出中标注"已驳回"，默认不计入汇总 | 是（教材室线下跟进） |
| [场景8-并发切换学期] | 双缓冲切换与在途写冲突 | 高 | 切换单事务+乐观校验；在途请求按旧学期快照完成 | 否 |

## 十三、验证目标

| 验证项 | 方式 | 通过标准 |
|--------|------|----------|
| 5 角色功能走查 | 按角色账号逐一走查功能列表 P0 | P0 全通过 |
| 数据隔离 | 越权矩阵（秘书×别院/教师×他人/学生×他人/供货商×学生字段） | 全部拒绝且有审计记录 |
| 窗口引擎 | 自动开关/延长/提前截止场景演练 | 状态流转正确、通知自动弹出 |
| 通知确认 | 未确认重发 5 轮、确认后停止 | notice_record 时间线完整 |
| 导入幂等 | 同文件重复上传 | 无重复数据、变更正确合并 |
| 性能 | 上万行导入/导出 | 无超时，进度可见 |

## 十四、PRD 风险和下一步

**风险**
1. 订阅消息授权率不可控 → 「收到」点击同时请求授权+授权引导+unauthorized 如实统计（R1）；
2. 小程序个人主体类目受限（G3）→ 小程序仅教师/学生，Web 端走学校主体不受影响；
3. 移交学校重部署漂移 → 相对路径+配置外置+部署手册（R6）。

**下一步**
1. M1：Web 脚手架 + RBAC 动态路由 + 登录/强制改密（配套 skill 见 04 号文档 §5）；
2. M1 末：与 textbook-order-server 冻结接口契约，回填本 PRD 字段规范表；
3. M2-M5 按 02 号文档里程碑推进，联调方案见 04 号文档 §5.3。
