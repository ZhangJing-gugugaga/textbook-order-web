# school：移交学校重部署，仅改此处路径与后端地址
#
# VITE_BASE 必须与校方实际部署路径一致（构建后自检：
#   grep -o 'src="[^"]*"' dist/index.html，前缀应与部署路径相同）：
#   · 独立子域名或独立站点根目录 → `/`
#   · 主域子路径（默认）→ `/textbook/`
VITE_BASE=/textbook/
VITE_PROXY_TARGET=http://127.0.0.1:8080
