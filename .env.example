# 环境变量样例（仅两项前端配置，无密钥；SPEC §3）
#
# 本地开发：无需任何 env 文件——`npm run dev`（Vite development 模式）默认
#   base=/ + 挂 vite-plugin-mock，开箱即用。
#
# trial（moonzj.com 试运行，子路径部署）→ 见 .env.trial
VITE_BASE=/textbook/
VITE_MOCK=false
VITE_PROXY_TARGET=http://127.0.0.1:8080
#
# school（移交学校重部署，仅改路径）→ 见 .env.school
# VITE_BASE=/textbook/
# VITE_MOCK=false
# VITE_PROXY_TARGET=http://127.0.0.1:8080
#
# 契约冻结后切真实后端：VITE_MOCK=false + VITE_PROXY_TARGET 指向 Spring Boot，
# 页面代码零改动（Q5）。
