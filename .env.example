# 环境变量样例（前端仅两项配置，无密钥；SPEC §3）
#
# 接口基础地址：前端一律相对路径 '/api'（硬约束，零域名硬编码）。
#   - 本地开发/预览：Vite dev/preview server 代理 '/api' → VITE_PROXY_TARGET
#   - 试运行/移交：Nginx 反代 '/api/' → 后端 8080（前端无需改代码）
#
# 本地开发：无需任何 env 文件——`npm run dev` 默认 base=/ + 代理到 http://127.0.0.1:8080
VITE_BASE=/
VITE_PROXY_TARGET=http://127.0.0.1:8080
#
# trial（moonzj.com 试运行，子路径部署）→ 见 .env.trial
#   VITE_BASE=/textbook/
#   VITE_PROXY_TARGET=http://127.0.0.1:8080
#
# school（移交学校重部署，仅改路径/后端地址）→ 见 .env.school
