# trial：试运行环境（线上 https://textbooksorder.moonzj.com），真实后端（Nginx 反代 /api/）
#
# VITE_BASE 必须与部署路径一致，否则静态资源 404：
#   · 独立子域名根目录（当前线上形态）→ `/`
#   · 主域子路径（如 moonzj.com/textbook/）→ `/textbook/`
# 线上实测为子域名根目录，故取 `/`。若改回子路径部署，同步改这里与 Nginx 的 location。
VITE_BASE=/
VITE_PROXY_TARGET=http://127.0.0.1:8080
